---
all:
  children:
    production:
      hosts:
        baudrate.tw:
          ansible_user: root
        # Replace with your server's IP or hostname
        # baudrate.example.com:
        #   ansible_user: root
