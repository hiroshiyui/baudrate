<!DOCTYPE html>
<html
  lang={String.replace(assigns[:locale] || "en", "_", "-")}
  data-theme-light={assigns[:theme_light] || "light"}
  data-theme-dark={assigns[:theme_dark] || "dark"}
>
  <head>
    <meta charset="utf-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1" />
    <meta name="csrf-token" content={get_csrf_token()} />
    <meta name="vapid-public-key" content={Baudrate.Setup.get_setting("vapid_public_key") || ""} />
    <meta name="theme-color" content="#570df8" />
    <link rel="manifest" href={~p"/site.webmanifest"} />
    <.live_title default="Baudrate" suffix=" · Baudrate">
      {assigns[:page_title]}
    </.live_title>
    <link rel="icon" type="image/svg+xml" href={~p"/favicon.svg"} />
    <%= if assigns[:feed_site] do %>
      <link
        rel="alternate"
        type="application/rss+xml"
        title={gettext("RSS Feed")}
        href={~p"/feeds/rss"}
      />
      <link
        rel="alternate"
        type="application/atom+xml"
        title={gettext("Atom Feed")}
        href={~p"/feeds/atom"}
      />
    <% end %>
    <%= if assigns[:feed_board_slug] do %>
      <link
        rel="alternate"
        type="application/rss+xml"
        title={gettext("Board RSS Feed")}
        href={~p"/feeds/boards/#{assigns[:feed_board_slug]}/rss"}
      />
      <link
        rel="alternate"
        type="application/atom+xml"
        title={gettext("Board Atom Feed")}
        href={~p"/feeds/boards/#{assigns[:feed_board_slug]}/atom"}
      />
    <% end %>
    <%= if assigns[:linked_data_json] do %>
      {raw("<script type=\"application/ld+json\">" <> @linked_data_json <> "</script>")}
    <% end %>
    <%= for {name, content} <- (assigns[:dc_meta] || []) do %>
      <meta name={name} content={content} />
    <% end %>
    <link phx-track-static rel="stylesheet" href={~p"/assets/css/app.css"} />
    <script phx-track-static type="text/javascript" src={~p"/assets/js/app.js"}>
    </script>
  </head>
  <body class="min-h-screen flex flex-col">
    <a
      href="#main-content"
      class="sr-only focus:not-sr-only focus:absolute focus:z-[100] focus:p-4 focus:bg-base-100 focus:text-base-content"
    >
      {gettext("Skip to main content")}
    </a>
    {@inner_content}
  </body>
</html>
