defmodule BaudrateWeb.RemoteFollowHTML do
  @moduledoc """
  The page `BaudrateWeb.RemoteFollowController` renders.

  See the `remote_follow_html` directory for the template.
  """
  use BaudrateWeb, :html

  embed_templates "remote_follow_html/*"
end
