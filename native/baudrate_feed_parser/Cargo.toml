[package]
name = "baudrate_feed_parser"
version = "0.1.0"
edition = "2021"

[lib]
name = "baudrate_feed_parser"
crate-type = ["cdylib"]

[dependencies]
feedparser-rs = { version = "0.7", default-features = false }
rustler = "0.38"
